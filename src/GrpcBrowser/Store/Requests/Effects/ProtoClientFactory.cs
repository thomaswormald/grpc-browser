namespace GrpcBrowser.Store.Requests.Effects
{
    public class ProtoClientFactory
    {
        // Get the IServiceProvider, try and get the required type. If that fails, use Activator.CreateInstance
    }
}
